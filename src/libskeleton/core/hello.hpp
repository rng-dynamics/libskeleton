#pragma once

#include <string>

// Namespace (and module names) start with the library name without the `lib`
// prefix.
namespace skeleton::core {

std::string GetHello();

} // namespace skeleton::core
